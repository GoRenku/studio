{
  "name": "@gorenku/studio-cli",
  "version": "0.1.0",
  "description": "Agent and human command surface for Renku Studio projects",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "renku": "dist/cli.js"
  },
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src tests",
    "type-check": "tsc --noEmit",
    "test": "vitest run --pool=threads --poolOptions.threads.singleThread",
    "test:integration": "vitest run --config vitest.integration.config.ts --pool=threads --poolOptions.threads.singleThread",
    "test:typecheck": "tsc --project tsconfig.vitest.json --noEmit",
    "check:all": "pnpm type-check && pnpm lint && pnpm test:typecheck",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "files": [
    "dist"
  ],
  "packageManager": "pnpm@11.7.0",
  "dependencies": {
    "@gorenku/studio-diagnostics": "workspace:*",
    "@gorenku/studio-core": "workspace:*",
    "meow": "^14.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@eslint/js": "^9.36.0",
    "@types/node": "^24.10.1",
    "@typescript-eslint/parser": "^8.18.2",
    "@vitest/ui": "^3.2.4",
    "eslint": "^8",
    "eslint-config-prettier": "^10.1.8",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
