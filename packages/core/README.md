# @gorenku/studio-core

Renku Studio domain package.

This package is the home for Renku Studio-specific project, workflow, queue, task, take, and projection primitives. It intentionally starts separate from the existing `@gorenku/core` blueprint/build system so Renku Studio can grow around its own domain model.
