---
import { Image } from 'astro:assets';

import githubMark from '../assets/icons/github-mark.svg';
import logo from '../assets/renku-logo.svg';
import { site } from '../data/site';
---

<header class="site-header">
  <div class="container header-inner">
    <a href="#top" class="brand">
      <Image src={logo} alt="" width="34" height="38" loading="eager" />
      <span class="brand-name">renku</span>
    </a>
    <nav class="site-nav" aria-label="Main">
      <a href="#features">Features</a>
      <a href="#workflow">Workflow</a>
      <a href="#audience">Who it’s for</a>
    </nav>
    <div class="header-actions">
      <a
        href={site.githubUrl}
        class="github-link"
        aria-label="Renku Studio on GitHub"
        target="_blank"
        rel="noreferrer"
      >
        <Image src={githubMark} alt="" width="20" height="20" loading="eager" />
      </a>
      <a href={site.studioUrl} class="btn btn-primary header-cta">Download Studio</a>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    inset-inline: 0;
    top: 0;
    z-index: 50;
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    background: color-mix(in srgb, var(--ink-950) 72%, transparent);
    border-bottom: 1px solid rgba(244, 239, 230, 0.06);
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 2rem;
    height: 4.25rem;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
  }

  .brand-name {
    font-family: var(--font-display);
    font-size: 1.45rem;
    font-weight: 600;
    letter-spacing: -0.01em;
  }

  .site-nav {
    display: flex;
    gap: 1.75rem;
    margin-inline-start: auto;
    font-size: 0.92rem;
    color: var(--paper-300);
  }

  .site-nav a {
    transition: color 0.2s ease;
  }

  .site-nav a:hover {
    color: var(--amber-400);
  }

  .header-actions {
    display: flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 0.75rem;
  }

  .github-link {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 999px;
    opacity: 0.78;
    transition:
      opacity 0.2s ease,
      background 0.2s ease;
  }

  .github-link:hover {
    opacity: 1;
    background: rgba(244, 239, 230, 0.08);
  }

  .github-link img {
    display: block;
    width: 1.25rem;
    height: 1.25rem;
  }

  .header-cta {
    padding: 0.6rem 1.15rem;
    font-size: 0.88rem;
  }

  @media (max-width: 640px) {
    .site-nav {
      display: none;
    }

    .header-inner {
      justify-content: space-between;
    }
  }
</style>
